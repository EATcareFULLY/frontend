import React from "react";
import {observer} from "mobx-react";

const Ranking = observer(() => {

    return (
        <div>
            <h1>Ranking</h1>
        </div>
    );
});

export default Ranking;